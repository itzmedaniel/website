/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // enable class-based dark mode so ThemeContext can toggle it
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: { extend: {} },
  plugins: [],
}

